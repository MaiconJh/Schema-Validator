package com.maiconjh.schemacr.schemes;

/**
 * Source channel used to register a schema in the runtime registry.
 */
public enum SchemaRegistrationSource {
    UNKNOWN,
    AUTOLOAD,
    API,
    SKRIPT
}
